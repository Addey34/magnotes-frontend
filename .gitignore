/node_modules
/dist
/firebase-dist

# Claude Code: local/personal settings (shared settings go in settings.json)
.claude/settings.local.json
CLAUDE.md

# Local environment (never commit — VITE_API_URL lives in .env.production, which
# is a public URL and IS committed on purpose)
.env
.env.local
.env.*.local

# Firebase deploy cache
.firebase/

# Logs
*.log

# Playwright artifacts
/test-results
/playwright-report
/blob-report
# Local/operator files never published by the public frontend repo
AGENTS.md
CLAUDE.md
.claude/
.codex/
.agents/
docs/

# All environment files are local; only templates may be committed
.env
.env.*
!.env.example
!.env.production.example
