# Frontend build config (loaded by client/vite.config.ts, envDir = repo root).
# The API now lives on its own subdomain (Nakama VM, behind Caddy); the frontend
# is served statically from Firebase Hosting at https://magnotes.adrianguichard.dev.
VITE_API_URL=https://api-magnotes.adrianguichard.dev
